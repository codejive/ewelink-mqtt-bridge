{
  "name": "ewelink-mqtt-bridge",
  "version": "1.0.0",
  "description": "Bridge eWeLink cloud updates into a local MQTT broker",
  "main": "bridge.js",
  "type": "commonjs",
  "scripts": {
    "start": "node bridge.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ewelink-api": "^3.1.1",
    "mqtt": "^5.14.1"
  }
}
